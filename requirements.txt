# 以下是项目所依赖的库，如果程序运行有BUG，请检查现有库的版本冲突，或者使用Python 3.11及以上版本的虚拟环境运行本项目
# ----------------------------------------------------------------
python-docx
playwright
browser-use
# ----------------------------------------------------------------
# 在安装完毕后，请用终端（CMD或PowerShell）执行"playwright install"，以完成playwright的安装。
